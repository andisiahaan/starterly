import './bootstrap';

// Alpine.js is handled separately:
// - For Livewire layouts (app, admin/app): Livewire includes Alpine automatically
// - For non-Livewire layouts (main, plain): Alpine CDN is loaded in the blade template
